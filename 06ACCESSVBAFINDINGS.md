# نتائج فحص أكواد VBA في ملف الأكسس الأصلي (ahmed_lab.mdb / lab.mdb)

هذا الملف يوثّق كل ما تم اكتشافه فعليًا (بالفحص المباشر، وليس تخمينًا) من أكواد VBA المخفية خلف
ملف الأكسس الأصلي: 5 موديولات مستقلة، 3 ماكروهات، وكود خلف 47 فورمًا (من أصل 166) تم تصديره عبر
أداة Database Documenter في Access. لم تُفحَص التقارير الـ93 بعد.

## ملخص تنفيذي سريع

- **لا يوجد أي منطق طبي أو مالي كان مفقودًا بالكامل من التطبيق الجديد** — البيانات الأساسية (كتالوج
  التحاليل، الأسعار، المدى الطبيعي) كانت دائمًا في جداول عادية وتم استخراجها بالكامل من قبل.
- **لكن اكتُشفت صيغ حسابية حقيقية** كان النظام الأصلي يحسبها تلقائيًا، ونحتاج نضيفها لتوفير مجهود
  الفني (تفاصيلها في القسم الأول تحت).
- **جزء كبير من الأكواد (موديولين كاملين) هو نظام حماية من النسخ (DRM)** لا علاقة له بمنطق المعمل
  إطلاقًا، ومفيش داعي لنقله للتطبيق الجديد.

---

## 1) صيغ حسابية مكتشَفة (تستحق التنفيذ في التطبيق)

### تحليل CBC (صورة الدم الكاملة) — فورم "CBC"

النظام الأصلي كان يحسب القيم دي **تلقائيًا** بمجرد إدخال WBC وRBC وHB والنسب المئوية للتفريق:

| المعيار | الصيغة |
|---|---|
| HCT (الهيماتوكريت) | `HB × 3.1` |
| Color Index (دليل اللون) | `(HB ÷ 16) × 100` |
| MCH | `(HB × 10) ÷ RBC` |
| MCHC | `(HB × 100) ÷ HCT` |
| MCV | `(HCT × 10) ÷ RBC` |
| Basophil Absolute Count | `Baso% × WBC × 10` |
| Eosinophil Absolute Count | `Eso% × WBC × 10` |
| Lymphocyte Absolute Count | `Lymph% × WBC × 10` |
| Monocyte Absolute Count | `Mono% × WBC × 10` |
| Segmented/Neutrophil Absolute Count | `Seg% × WBC × 10` |

المصدر: `Form_CBC` — أحداث `ht_GotFocus`, `index_GotFocus`, `mch`/`mchc`/`mcv` (نفس النمط)،
`basor_GotFocus`, `esor_GotFocus`, `lymr_GotFocus`, `monor_GotFocus`, `segr` (نفس النمط).

### تصفية الكرياتينين (Creatinine Clearance) — فورم "Clerance"

```
Creatinine Clearance = (كرياتينين البول × حجم البول) ÷ (كرياتينين الدم × 1440)
```
(1440 = عدد دقائق اليوم؛ معادلة تصفية الكرياتينين المعيارية من عينة بول 24 ساعة)

فيه كمان قيمة تانية محسوبة في نفس الفورم: `ucrea = (كرياتينين البول × حجم البول) ÷ 100000`
(على الأغلب "إفراز الكرياتينين اليومي" بوحدة مختلفة — الاسم بالظبط مش مؤكد 100%).

### صيغة مجهولة الهوية (تحتاج تأكيد لاحقًا)

فورمين مختلفين (أحدهما قريب من فورم "CSF" وواحد قريب من "concn") فيهم نفس النمط:
```
القيمة الناتجة = (قيمة1 × قيمة2)، مقرَّبة لمنزلة عشرية واحدة
```
أسماء الحقول في الكود عامة (`Text0`, `Text2`, `Text4`) فمش واضح بالظبط لأي تحليل هي — تحتاج نشوف
تسمية الحقول الفعلية على الفورم نفسه (مش موجودة في الكود، موجودة في تصميم الفورم).

---

## 2) قواعد تحقق وسلوكيات مفيدة (أصغر لكن حقيقية)

- **تحقق من مطابقة النوع للتحليل**: فورم **BHCG** (تحليل حمل) بيقفل نفسه تلقائيًا لو نوع المريض
  "ذكر" (`Form_Load`: `If Forms![ID]![sex] = "male" Then DoCmd.Close`).
- **اختصارات كتابة سريعة**: في فورمات زي **ABO** والبلهارسيا (bilh)، كتابة حرف "n" في حقل النتيجة
  يحوّلها تلقائيًا لـ"Negative"، وحرف "p" لـ"Positive".
- **ترقيم فواتير تلقائي**: فورم "Acount" بيحسب `MAX(Invoice_no) + 1` — **نفس المنطق المُطبَّق بالفعل**
  في تطبيقنا الحالي، لا حاجة لأي تغيير.
- **نمط تنبيه القيم الشاذة (عام على كل التحاليل تقريبًا)**: أي حقل رقمي بيتقارن بحدّين مخفيين (H وL)
  خاصين بكل مريض حسب سنه ونوعه، ولو القيمة برّه المدى بيظهر MsgBox "The result is High/Low" وتتحط
  علامة `*` جنب القيمة. هذا **يطابق مفهوميًا** ما يفعله تطبيقنا الحالي (حساب `flag` = طبيعي/مرتفع/منخفض)
  — فرق التنفيذ فقط (عمود منفصل عندنا بدل نجمة ملصقة بالنص).

---

## 3) نظام حماية من النسخ (DRM) — لا علاقة له بمنطق المعمل

اكتُشف في 4 من الموديولات الخمسة المستقلة:

- **Module2**: يعطّل خاصية `AllowBypassKey` (تمنع فتح البرنامج بالضغط على Shift للوصول للتصميم/الكود
  مباشرة) — آلية إخفاء عن المستخدم النهائي.
- **Module3**: نظام **قفل الجهاز الواحد**. يقرأ الرقم التسلسلي لقرص C: (عبر Windows API)، ويقارنه
  برقم محفوظ في جدول "beta". لو الرقمين ما يتطابقوش (يعني اتنسخ الملف لجهاز تاني)، البرنامج يغلق
  نفسه فورًا (`DoCmd.Quit`) عبر دالة `security()`.
- **Module4**: كود بسيط بيقرأ اسم المستخدم من قائمة منسدلة في فورم الدخول — مفيش تعقيد.
- **Tashfer**: خوارزمية تشفير بدائية جدًا (صنيعة يدوية، غير آمنة تقنيًا)، على الأغلب لتشفير الرقم
  التسلسلي قبل حفظه في جدول "beta" عشان محدش يعدّله يدويًا بسهولة.
- **فورم "a"** (فورم التفعيل/التسجيل): بيعرض الرقم التسلسلي والفولوم ليبل للمستخدم في Label5/Label6،
  ويطلب منه يكتب "كود تفعيل" في `Text1`. **الكود المطلوب هو ببساطة الرقم المعروض + 100**
  (`Val(Me.Text1) = Val(Me.Label6) + 100`) — حماية ضعيفة جدًا وسهلة الالتفاف عليها فعليًا.

**الخلاصة**: هذا كله نظام حماية بناه مطوّر الأكسس الأصلي لمنع نسخ برنامجه لجهاز تاني بدون ترخيص جديد.
بما إن التطبيق الجديد ملكك بالكامل ومفتوح المصدر عندك، **لا داعي لأي نظام مشابه** إلا لو رغبت في حماية
مماثلة (أحدث وأأمن) بنفسك مستقبلًا كقرار عملك الخاص.

---

## 4) ميزة موجودة في الأصل لكنها خارج نطاق التطبيق الحالي

فورمات **culture، culture2، Antibiotic، ORGANISM** تشكّل منظومة **ميكروبيولوجي كاملة**:
تسجيل نوع العينة (Material) + تحديد الكائن الحي المعزول (Organism) + شبكة حساسية المضادات الحيوية،
مع تحقق من إدخال البيانات المطلوبة (`MsgBox "Enter Material"` / `"Enter Organism"`) وإمكانية إضافة
كائنات جديدة للقائمة (`NotInList` → فتح فورم "ORGANISM" لإضافة كائن جديد).

هذه الميزة كانت معروفة كجزء من النطاق الكامل (نسخة .NET) لكنها **ليست ضمن النطاق الأساسي** التي
اخترته لنسخة بايثون في البداية. تحتاج قرار منك: تُضاف كميزة جديدة أم تبقى خارج النطاق حاليًا.

---

## 5) فورم "Calculation" — ليس حسابًا رياضيًا، بل شاشة تقارير وفلترة

بالرغم من الاسم، فورم "Calculation" هو شاشة فلترة لفتح تقارير جاهزة (`testco`, `incomplet`, `C2`,
`Calculation`, `Cal`) حسب اسم التحليل / الطبيب المحوِّل / جهة الإحالة — يطابق مفهوميًا شاشة
"التقارير والإحصائيات" المذكورة سابقًا كجزء من النطاق الكامل (.NET) وغير موجودة بعد في نسخة بايثون.

---

## 6) نسبة التغطية الفعلية حتى الآن

| العنصر | العدد الكلي | تم فحصه فعليًا |
|---|---|---|
| موديولات VBA مستقلة | 5 | 5 (100%) |
| ماكروهات | 3 | 0 (لم تُفحص بعد) |
| فورمات (بما فيها الكود خلفها) | 166 | 47 (~28%) |
| تقارير | 93 | 0 (لم تُفحص بعد) |
| استعلامات | 433 | فُهمت هيكليًا فقط، لا مراجعة فردية |

**فورمات يُنصَح بمراجعتها بالأولوية لو حبينا نكمّل** (الأرجح تحتوي صيغًا حسابية معيارية معروفة):
وظائف الكبد (Liver Function)، وظائف الكلى (Kidney Function)، الدهون (Lipid Profile)، الغدة الدرقية
(Thyroid)، تحليل البول (Urine Analysis).

---

## 7) الخطوات التالية المقترحة

1. تنفيذ صيغ CBC وتصفية الكرياتينين في شاشة "نتائج التحاليل" (حساب تلقائي عند إدخال القيم الأساسية).
2. إضافة اختصارات الكتابة السريعة (n/p) للحقول النصية.
3. إضافة تحقق نوع المريض المناسب للتحليل (تنبيه أو منع، حسب الأفضل) لتحاليل مثل BHCG.
4. (اختياري) استخراج ومراجعة كود الفورمات ذات الأولوية المذكورة أعلاه بحثًا عن صيغ حسابية إضافية.
5. (اختياري، نطاق أكبر) بناء منظومة الميكروبيولوجي (عينة + كائن حي + حساسية مضادات) كميزة جديدة.
